#include<stdio.h>
int main()
{
	int a,b,multiple;
	printf("enter the two numbers");
	scanf("%d %d",&a,&b);
	multiple=a*b;
	printf("the multiple of two numbers is=%d",multiple);
	return 0;
}
