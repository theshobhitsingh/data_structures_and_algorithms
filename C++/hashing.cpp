#include <iostream>
using namespace std;

int main()
{
    int arr[100];
    int n = 100;
    int key;
    cout << "Enter the value for key: ";
    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
    }
     cout << "Enter the value for address: ";
    int add = key % 10;
    while (a[pos])
    {
        /* code */
    }
    
    return 0;
}